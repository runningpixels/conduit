// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ContentAnnotation } from "./content_annotation";
import type { ProviderError } from "./provider_error";
import type { ProviderUsage } from "./provider_usage";

export type ProviderEvent = { "kind": "messageStart", requestId: string, index: number, } | { "kind": "contentBlockStart", requestId: string, blockId: string, index: number, blockKind: string, } | { "kind": "contentDelta", requestId: string, blockId: string, index: number, content: string, } | { "kind": "reasoningDelta", requestId: string, blockId: string, index: number, content: string, } | { "kind": "contentBlockStop", requestId: string, blockId: string, index: number, } | { "kind": "toolCallStart", requestId: string, toolCallId: string, index: number, toolId: string, name: string, } | { "kind": "toolCallDelta", requestId: string, toolCallId: string, index: number, content: string, } | { "kind": "toolCallComplete", requestId: string, toolCallId: string, index: number, arguments: Record<string, unknown>, } | { "kind": "usage", requestId: string, usage: ProviderUsage, } | { "kind": "ping", requestId: string, } | { "kind": "messageComplete", requestId: string, index: number, finishReason: string, } | { "kind": "error", requestId: string, error: ProviderError, } | { "kind": "searchSources", requestId: string, index: number, 
/**
 * Pass-through raw sources from `web_search_call.action.sources`.
 * The renderer shapes for display; the adapter does not parse.
 */
sources: Array<Record<string, unknown>>, } | { "kind": "citation", requestId: string, 
/**
 * Block the citation is attached to. The renderer walks these in
 * `start_index`/`end_index` order and inserts inline `[n]` markers.
 */
blockId: string, index: number, annotation: ContentAnnotation, } | { "kind": "searchCost", requestId: string, index: number, 
/**
 * Number of web_search tool calls the model issued in this response.
 */
toolCalls: number, } | { "kind": "searchUnavailable", requestId: string, index: number, 
/**
 * Why the hosted search tool was stripped or refused. Stable codes
 * the renderer can branch on; `message` is human-readable.
 */
code: string, message: string, } | { "kind": "agentPhase", requestId: string, 
/**
 * Human-readable label for the current phase.
 */
label: string, 
/**
 * Round number (1-based).
 */
round: number, 
/**
 * Total rounds or 0 if unknown.
 */
totalRounds: number, 
/**
 * Sub-phase identifier: "thinking", "executing_tools", "reviewing", "connecting", "finalizing"
 */
subPhase: string, } | { "kind": "toolExecutionStarted", requestId: string, toolCallId: string, toolName: string, } | { "kind": "toolExecutionFinished", requestId: string, toolCallId: string, toolName: string, 
/**
 * True if the tool execution failed.
 */
isError: boolean, 
/**
 * Optional error message.
 */
error?: string, };
